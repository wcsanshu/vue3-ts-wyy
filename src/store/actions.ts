const actions = {
  // 修改正在播放状态
  setPlayingState(state, playing) {
    state.playing = playing
  },
  // 设置顺序播放列表
  setSquenceList(state, list) {
    state.sequenclList = list
  },
  // 设置播放模式
  setPlayMode(state,mode) {
    state.
  }
}
